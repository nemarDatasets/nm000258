# sourcedata — original distribution

Unmodified original files for this dataset as distributed upstream, retrieved via MOABB.
Included so the deposit carries its own provenance instead of depending on an external
URL remaining reachable.

| | |
|---|---|
| MOABB class | `moabb.datasets.Pressel2016` (moabb 1.5.0dev0) |
| Retrieved | 2026-08-13 |
| Files | 15 |
| Total size | 1771.3 MB |
| License | other-open |
| Upstream DOI | 10.1117/12.2255697 |

File **contents** are byte-for-byte as retrieved; no conversion was applied. Filenames are the upstream names: MOABB's local cache stores each download as `<name>/<md5>-<name>`, and that cache-only hash prefix and wrapper directory were removed.
Per-file SHA-256 checksums are in `sourcedata_provenance.json`.
